# === API Core ===
fastapi==0.112.1
uvicorn==0.30.6
pydantic==2.7.1
python-dotenv==1.0.1

# === Base de datos (PostgreSQL async) ===
psycopg[binary]==3.3.3
psycopg-pool==3.3.0

# === Conexión sync (usado por seed.py y trazabilidad_core) ===
psycopg2-binary==2.9.11

# === Configuración ===
PyYAML==6.0.2

# === Chatbot (HTTP client para LLM API) ===
# === Procesamiento y Seguridad ===
pyclamd==0.4.0
lxml==5.3.0
python-magic==0.4.27

# === Autenticación (OAuth2 / JWT) ===
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
